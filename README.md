# Linux and Git Command Cheat Sheet

## ls
Lists files and directories in the current directory.

```bash
ls -la
```

## pwd
Prints the current working directory (shows you which folder you're in).

```bash
pwd
```

git checkout -b add-pwd-command
Add-Content README.md "`n## pwd`nPrints the current working directory (shows you which folder you're in).`n`n``````bash`npwd`n```````n"
git add README.md
git commit -m "Add pwd command to cheat sheet"
git checkout main
git merge add-pwd-command
git push
## pwd
Prints the current working directory (shows you which folder you're in).

```bash
pwd
```

